/**
 * Idempotent seed — 3 agencies, 3 months of data (docs/project.md).
 * Implemented after schema + DB client (plan Tasks 2–3+).
 */
async function main(): Promise<void> {
  // Task 2+: implement seed
}

void main();
